$("#flash").html("<%= escape_javascript(flash[:error]) %>");
$("#flash").show(300);

<% if @user_path  %>
	<% publish_to @user_path do %>
	  	  $("table.table").append("<%= j render(@task) %>");
	  	  $("tr#<%= dom_id(@task) %>").effect('highlight');
	<% end %>
<% end %>	
