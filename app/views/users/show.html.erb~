<table class="profile">
    <tr class="sidebar round">
      <strong>Name</strong> <%= @user.name %><br />
      <strong>Email</strong> <%= @user.email %><br />
      <strong>URL</strong> <%= link_to user_path(@user), @user %><br />
      <strong>Tasks</strong> <%= @user.tasks.count %>
    </tr>
  <tr>
    <td class="main">
      <% unless @user.tasks.empty? %>
        <table class="tasks" summary="User tasks">
             	<% @user.tasks.each do |task| %>
             	<tr>
		  <td class="task">
		    <b> <span class="name"><%= task.title %></span> </b><br>
		    <span class="content"><%= task.content %></span><br>
		    <span class="timestamp">
		      Created <%= time_ago_in_words(task.created_at) %> ago.
		    </span>
		  </td>
		</tr>
		<% end %>
        </table>
      <% end %>
    </td>
  </tr>
</table>
